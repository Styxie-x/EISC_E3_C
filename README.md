# EISC_E3_C
 - TD1 finished i guess
 - TD2 only finished 2 exos
 - TD3 finished i guess
 - TD4 finished i think;
 - TD5 doing

TODO : 
# EISC_E3_C
 - TD1 finished i guess
 - TD2 only finished 2 exos
 - TD3 finished i guess
 - TD4 finished i think;
 - TD5 doing

TODO :
- types int, float, char ....etc;
- arrays
- functions();
- function return;
- function parameters;
- for, if, while, do-while, break,
- structures (struct)
- typedef
- enums (enum)
- pointers basics: *, & 
- pointer arithmetic
- passing pointers to functions
- arrays & pointers relation
- strings: char[] and '\0'
- preprocessor: #define, #include, macros
- header files (.h) and prototypes
- separate compilation (.c/.h)
- local vs global variables
- scope and lifetime
- memory model: stack vs heap (intro)
- dynamic allocation: malloc/free
- recursion basics
- switch / case / default
- operators (+ - * / % ++ --)
- comparison operators (== != < > <= >=)
- logical operators (&& || !)
- bitwise operators (& | ^ << >>)
- type casting (int)x, (float)x
- const keyword
- reading/writing files (fopen, fclose)

- static variables (static local + static global)
- using malloc to fix null pointer errors
- checking malloc return value (NULL)
- dynamic arrays: int* p = malloc(N*sizeof(int))
- accessing dynamic arrays p[i]
- copying dynamic arrays via pointers
- modifying arrays through multiple pointers (p1, p2)
- freeing memory at the end (free)
- function returning pointer to dynamic array
- what happens with negative sizes in allocation
- implementing min(float* tab, int N)
- formulas for filling arrays
- duplicating arrays: create_double()
- concatenating two arrays: concat()
- avoiding memory leaks
- difference between static and dynamic arrays
- pointer aliasing (two pointers to same array)

- ASCII table for characters '0'–'9', 'A'–'Z', 'a'–'z'
- null-terminated strings ('\0')
- static char arrays: char a[11]
- string literals vs modifiable arrays
- immutable strings (char* c = "Hi" cannot be changed)
- modifying strings stored in arrays
- implementing string length: my_strlen()
- comparing my_strlen with strlen
- removing spaces in a string (my_squeeze_string)
- returning dynamically allocated strings
- scanf with "%s" (stops at space)
- scanf return values
- reading input safely
- writing to files (fprintf with mode "w")
- reading files character-by-character (fgetc)
- printing characters (putchar)
- file modes: "r", "w", "a", "rb", "wb"
- checking if a file opened successfully
- secure input reading (my_scanf)
- clearing input buffer (getchar loop)
- validating float input
- handling multi-word input (limits of %s)

- function pointers basics: int (*f)(int)
- using function pointer parameters
- applying a function to each element of an array
- implementing transform_tableau(tab, N, f)
- writing functions that return squared or cubed values
- compare(a, b) returning 1, -1, 0
- finding maximum element index with a comparator: maxtab()
- effects of inverted comparison logic
- qsort usage: qsort(array, N, sizeof(int), cmpr)
- writing qsort comparator: int cmpr(const void*, const void*)
- casting void* to int* inside comparator
- importance of freeing allocated memory
- importance of closing open files


